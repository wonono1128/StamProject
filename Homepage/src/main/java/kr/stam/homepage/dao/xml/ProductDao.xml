<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="kr.stam.homepage.dao.ProductDao">

	<!-- 다음 게시판 글 번호 알아보기 -->
	<select id="nextNum" resultType="Integer">
	    select max(productCode) from product 
	</select>
	
		<!-- 리스트 -->	
<!-- 	<select id="list" resultType="kr.stam.homepage.dto.ProductDto">
	  select * from product where menuContents="2021" order by productCode
	</select> -->
	
	<select id="list" resultType="kr.stam.homepage.dto.ProductDto">
	  select * from product where menuContents=#{menuContents} order by productCode
	</select>
	
</mapper>